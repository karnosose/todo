import { makeStyles } from '@material-ui/core/styles';

const useStyle = makeStyles({
    todoForm: {
        width: 400,
        margin: 'auto',
    },
    todoFormItem: {
        width: "100%",
    }
})

export default useStyle;
